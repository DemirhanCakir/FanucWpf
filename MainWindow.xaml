<Window x:Class="FanucWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FanucWpf"
        xmlns:viewmodels="clr-namespace:FanucWpf.ViewModels"
        mc:Ignorable="d"
        Title="Fanuc Robot Client" Height="450" Width="600">
    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Connection Panel -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Robot IP:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding IpAddress, UpdateSourceTrigger=PropertyChanged}" Width="120" IsEnabled="{Binding IsConnecte}"/>
            <TextBlock Text="Port:" VerticalAlignment="Center" Margin="10,0,5,0"/>
            <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Width="60" IsEnabled="{Binding IsConnected}"/>
            <Button Content="Connect" Command="{Binding ConnectCommand}" Margin="10,0,5,0" Padding="10,2"/>
            <Button Content="Disconnect" Command="{Binding DisconnectCommand}" Padding="10,2"/>
        </StackPanel>

        <!-- Send Message Panel -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Message:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding MessageToSend, UpdateSourceTrigger=PropertyChanged}" Width="300"/>
            <Button Content="Send" Command="{Binding SendMessageCommand}" Margin="10,0,0,0" Padding="10,2"/>
            <Button Content="Set" Command="{Binding SetRegisterCommand}" Margin="10,0,0,0" Padding="10,2"/>
            <Button Content="Get" Command="{Binding GetRegisterCommand}" Margin="10,0,0,0" Padding="10,2"/>
            <Button Content="GetPos" Command="{Binding GetPositionRegister}" Margin="10,0,0,0" Padding="10,2"/>
        </StackPanel>

        <!-- Log View -->
        <GroupBox Grid.Row="2" Header="Communication Log">
            <DockPanel>
                <ListView ItemsSource="{Binding LogEntries}" 
                          Name="LogListView"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.VerticalScrollBarVisibility="Visible"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </DockPanel>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <Ellipse Width="15" Height="15">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="Red"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="Fill" Value="Green"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>